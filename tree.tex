\RequirePackage{luatex85}
\documentclass{standalone}

\usepackage{tikz}

\usetikzlibrary{graphdrawing, graphdrawing.trees}

\begin{document}

\begin{tikzpicture}[tree layout]
\node {<Program>}
child { node {\textbf{BEGINPROG} \textit{BEGINPROG}}
 }
child { node {\textbf{PROGNAME} \textit{Factorial}}
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Variables>}
child { node {\textbf{VARIABLES} \textit{VARIABLES}}
 }
child { node {<VarList>}
child { node {\textbf{VARNAME} \textit{number}}
 }
child { node {<VarListPrim>}
child { node {\textbf{COMMA} \textit{,}}
 }
child { node {\textbf{VARNAME} \textit{result}}
 }
child { node {<VarListPrim>}
child { node {$\varepsilon$}
 }
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<Read>}
child { node {\textbf{READ} \textit{READ}}
 }
child { node {\textbf{LPAREN} \textit{(}}
 }
child { node {<VarList>}
child { node {\textbf{VARNAME} \textit{number}}
 }
child { node {<VarListPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{RPAREN} \textit{)}}
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<Assign>}
child { node {\textbf{VARNAME} \textit{result}}
 }
child { node {\textbf{ASSIGN} \textit{:=}}
 }
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{NUMBER} \textit{1}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<If>}
child { node {\textbf{IF} \textit{IF}}
 }
child { node {\textbf{LPAREN} \textit{(}}
 }
child { node {<Cond>}
child { node {<AndCond>}
child { node {<SimpleCond>}
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{VARNAME} \textit{number}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<Comp>}
child { node {\textbf{GEQ} \textit{>=}}
 }
 }
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{NUMBER} \textit{0}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
 }
child { node {<AndCondPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<CondPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{RPAREN} \textit{)}}
 }
child { node {\textbf{THEN} \textit{THEN}}
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<For>}
child { node {\textbf{FOR} \textit{FOR}}
 }
child { node {\textbf{VARNAME} \textit{i}}
 }
child { node {\textbf{ASSIGN} \textit{:=}}
 }
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{NUMBER} \textit{1}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{TO} \textit{TO}}
 }
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{VARNAME} \textit{number}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{DO} \textit{DO}}
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<Assign>}
child { node {\textbf{VARNAME} \textit{result}}
 }
child { node {\textbf{ASSIGN} \textit{:=}}
 }
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{VARNAME} \textit{result}}
 }
 }
child { node {<TermPrim>}
child { node {\textbf{TIMES} \textit{*}}
 }
child { node {<Atom>}
child { node {\textbf{VARNAME} \textit{i}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{ENDFOR} \textit{ENDFOR}}
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<Print>}
child { node {\textbf{PRINT} \textit{PRINT}}
 }
child { node {\textbf{LPAREN} \textit{(}}
 }
child { node {<ExpList>}
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{VARNAME} \textit{result}}
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExpListPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{RPAREN} \textit{)}}
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {$\varepsilon$}
 }
 }
 }
 }
child { node {<IfSeq>}
child { node {\textbf{ELSE} \textit{ELSE}}
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {<Instruction>}
child { node {<Print>}
child { node {\textbf{PRINT} \textit{PRINT}}
 }
child { node {\textbf{LPAREN} \textit{(}}
 }
child { node {<ExpList>}
child { node {<ExprArith>}
child { node {<Term>}
child { node {<Atom>}
child { node {\textbf{MINUS} \textit{-}}
 }
child { node {<Atom>}
child { node {\textbf{NUMBER} \textit{1}}
 }
 }
 }
child { node {<TermPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExprArithPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {<ExpListPrim>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{RPAREN} \textit{)}}
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {$\varepsilon$}
 }
 }
 }
child { node {\textbf{ENDIF} \textit{ENDIF}}
 }
 }
 }
 }
child { node {\textbf{ENDLINE} \textit{
}}
 }
child { node {<Code>}
child { node {$\varepsilon$}
 }
 }
 }
 }
 }
child { node {\textbf{ENDPROG} \textit{ENDPROG}}
 }
;
\end{tikzpicture}

\end{document}
%% Local Variables:
%% TeX-engine: luatex
%% End: